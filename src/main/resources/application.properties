# ── Repository type ───────────────────────────────────────────────────────────
# Allowed values: inmemory | jdbc
repository.type=inmemory

# ── H2 JDBC (only used when repository.type=jdbc) ─────────────────────────────
db.url=jdbc:h2:mem:librarydb;DB_CLOSE_DELAY=-1
db.user=sa
db.password=
